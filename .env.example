# Project environment sample for multi_search
# Copy to .env and adjust as needed

# Persist directory for Chroma vector DB (default if unset)
CHROMA_PERSIST_DIR=.chroma

# Unified debug switch for LCEL logs (default ON)
# Accepts: 1/true/yes/on (case-insensitive) to enable
MULTI_SEARCH_DEBUG=1

# Optional: Ollama base URL (if not default localhost)
# OLLAMA_BASE_URL=http://localhost:11434